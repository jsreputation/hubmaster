import { RouteToAbsolutePipe } from './route-to-absolute.pipe';

describe('RouteToAbsolutePipe', () => {
  it('create an instance', () => {
    const pipe = new RouteToAbsolutePipe();
    expect(pipe).toBeTruthy();
  });
});
