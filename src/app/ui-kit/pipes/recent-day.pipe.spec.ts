import { RecentDayPipe } from './recent-day.pipe';

describe('RecentDayPipe', () => {
  it('create an instance', () => {
    const pipe = new RecentDayPipe();
    expect(pipe).toBeTruthy();
  });
});
