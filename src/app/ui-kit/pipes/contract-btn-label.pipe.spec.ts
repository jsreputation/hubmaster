import { ContractBtnLabelPipe } from './contract-btn-label.pipe';

describe('ContractBtnLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new ContractBtnLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
