import { ToastrColorPipe } from './toastr-color.pipe';

describe('ToastrColorPipe', () => {
  it('create an instance', () => {
    const pipe = new ToastrColorPipe();
    expect(pipe).toBeTruthy();
  });
});
