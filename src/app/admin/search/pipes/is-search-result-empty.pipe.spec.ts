import { IsSearchResultEmptyPipe } from './is-search-result-empty.pipe';

describe('IsSearchResultEmptyPipe', () => {
  it('create an instance', () => {
    const pipe = new IsSearchResultEmptyPipe();
    expect(pipe).toBeTruthy();
  });
});
